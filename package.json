{
  "name": "newsletter-cli",
  "version": "1.0.0",
  "type": "module",
  "engines": { "node": ">=24.0.0" },
  "scripts": {
    "start": "node --env-file=.env src/cli.js run",
    "dry-run": "node --env-file=.env src/cli.js run --dry-run"
  },
  "dependencies": {
    "zod": "^3.23.0",
    "rss-parser": "^3.13.0",
    "nodemailer": "^6.9.0",
    "@anthropic-ai/sdk": "^0.39.0"
  }
}
